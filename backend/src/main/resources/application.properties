# Server
server.port=8080

# MySQL Database
spring.datasource.url=jdbc:mysql://schoolmanagement.cnoko4myoq0b.ap-south-1.rds.amazonaws.com:3306/school_erp_system?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=manager123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS Configuration
app.cors.allowed-origins=http://localhost:5173,http://school-management-deep.s3-website.ap-south-1.amazonaws.com

# JWT
jwt.secret=SchoolERPSecretKeyForJWTTokenGenerationAndValidationMustBeLongEnoughForHS256
jwt.expiration=86400000

# File upload
file.upload-dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
